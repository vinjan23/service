---
layout: '~/layouts/MarkdownLayout.astro'
title: Snapshot
network: Mainnet
icon: synternet
chain: synternet-1
version: v0.19.1
---
Height : 2929329 (173 MB)
---
```
sudo systemctl stop syntd
cp $HOME/.amber/data/priv_validator_state.json $HOME/.amber/priv_validator_state.json.backup
rm -rf $HOME/.amber/data
curl -L https://snapshot.vinjan.xyz./synternet/latest.tar.lz4 | lz4 -dc - | tar -xf - -C $HOME/.amber
mv $HOME/.amber/priv_validator_state.json.backup $HOME/.amber/data/priv_validator_state.json
sudo systemctl restart syntd
sudo journalctl -u syntd -f -o cat
```

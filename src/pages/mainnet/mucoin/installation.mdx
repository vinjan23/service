---
layout: '~/layouts/MarkdownLayout.astro'
title: Installation Node
icon: mucoin
network: Mainnet
chain: mucoin-1
version: rewards-v0.8.0
---

### Hardware requirements

The following requirements are recommended:

- 4 or more physical CPU cores
- At least 200GB of SSD disk storage
- At least 8GB of memory (RAM)
- At least 100mbps network bandwidth


### Install dependencies Required

```
sudo apt update && sudo apt upgrade -y && sudo apt install curl tar wget clang pkg-config libssl-dev jq build-essential bsdmainutils git make ncdu gcc git jq chrony liblz4-tool -y
```

### Install go

We are gonna use GO Version 1.26.5
If you already have GO installed you can skip this 

```
ver="1.26.5"
cd $HOME
wget "https://golang.org/dl/go$ver.linux-amd64.tar.gz"
sudo rm -rf /usr/local/go
sudo tar -C /usr/local -xzf "go$ver.linux-amd64.tar.gz"
rm "go$ver.linux-amd64.tar.gz"
echo "export PATH=$PATH:/usr/local/go/bin:$HOME/go/bin" >> ~/.bash_profile
source ~/.bash_profile
go version
```

### Install binary

```
cd $HOME
rm -rf mucoin
git clone https://github.com/dasgrid/mucoin.git
cd mucoin
git checkout rewards-v0.8.0
make install
```

### Init `Change <MONIKER> with ur Name`

```
mucoind init <MONIKER> --chain-id mucoin-1
```

### Download Genesis file and addrbook

- Genesis

```
curl -L https://snapshot.vinjan-inc.com/mucoin/genesis.json > $HOME/.mucoin/config/genesis.json
```
- Addrbook

```
curl -L https://snapshot.vinjan-inc.com/mucoin/addrbook.json > $HOME/.mucoin/config/addrbook.json
```

### Set Peer & Gas
```
peers="14b942af909ff8740c52bea456949a5de4be98e8@peer-mucoin.vinjan-inc.com:15756,32361fe4a8e26a1096261c031a951ed31bb07598@169.58.22.139:26656"
sed -i -e "s|^persistent_peers *=.*|persistent_peers = \"$peers\"|" $HOME/.mucoin/config/config.toml
sed -i -e "s/^minimum-gas-prices *=.*/minimum-gas-prices = \"0.01umuc\"/" $HOME/.mucoin/config/app.toml
```

### Config pruning
```
sed -i \
-e 's|^pruning *=.*|pruning = "custom"|' \
-e 's|^pruning-keep-recent *=.*|pruning-keep-recent = "100"|' \
-e 's|^pruning-keep-every *=.*|pruning-keep-every = "0"|' \
-e 's|^pruning-interval *=.*|pruning-interval = "20"|' \
$HOME/.mucoin/config/app.toml
```

### Indexer Off
```
sed -i 's|^indexer *=.*|indexer = "null"|' $HOME/.mucoin/config/config.toml
```

### create service file and start node

```
sudo tee /etc/systemd/system/mucoind.service > /dev/null <<EOF
[Unit]
Description=mucoin
After=network-online.target
[Service]
User=$USER
ExecStart=$(which mucoind) start
Restart=on-failure
RestartSec=3
LimitNOFILE=65535
[Install]
WantedBy=multi-user.target
EOF
```

- Enable Service and Start Node

```
sudo systemctl daemon-reload
sudo systemctl enable mucoind
```

### Use Snapshot or Statesync
<a href="https://vinjan-inc.com/mainnet/mucoin/snapshot" target="_blank">
  <button style="background-color: green; border: none; color: white; padding: 15px 32px; text-align: center; text-decoration: none; display: inline-block; font-size: 16px; margin: 4px 2px; cursor: pointer; border-radius: 10px; box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2), 0 6px 20px 0 rgba(0,0,0,0.19);" onmouseover="this.style.boxShadow='0 0 0 4px rgba(0,255,0,0.5)'" onmouseout="this.style.boxShadow='0 8px 16px 0 rgba(0,0,0,0.2), 0 6px 20px 0 rgba(0,0,0,0.19)'">Snapshot</button>
</a>

<a href="https://vinjan-inc.com/mainnet/mucoin/statesync" target="_blank">
  <button style="background-color: green; border: none; color: white; padding: 15px 32px; text-align: center; text-decoration: none; display: inline-block; font-size: 16px; margin: 4px 2px; cursor: pointer; border-radius: 10px; box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2), 0 6px 20px 0 rgba(0,0,0,0.19);" onmouseover="this.style.boxShadow='0 0 0 4px rgba(0,255,0,0.5)'" onmouseout="this.style.boxShadow='0 8px 16px 0 rgba(0,0,0,0.2), 0 6px 20px 0 rgba(0,0,0,0.19)'">Statesync</button>
</a>

- Start

```
sudo systemctl restart mucoind
sudo journalctl -u mucoind -f -o cat
```


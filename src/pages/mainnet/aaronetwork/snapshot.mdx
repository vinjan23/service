---
layout: '~/layouts/MarkdownLayout.astro'
title: Snapshot
network: Mainnet
icon: aaron
chain: aaronetwork
version: v1.0.0
---
( Height 271934 )
```
sudo apt install lz4 -y
sudo systemctl stop aaronetworkd
cp $HOME/.aaronetwork/data/priv_validator_state.json $HOME/.aaronetwork/priv_validator_state.json.backup
rm -rf $HOME/.aaronetwork/data 
curl -L https://snapshot.vinjan.xyz./aaron/latest.tar.lz4  | lz4 -dc - | tar -xf - -C $HOME/.aaronetwork
mv $HOME/.aaronetwork/priv_validator_state.json.backup $HOME/.aaronetwork/data/priv_validator_state.json
sudo systemctl restart aaronetworkd
sudo journalctl -u aaronetworkd -f -o cat
```

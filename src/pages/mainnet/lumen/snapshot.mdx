---
layout: '~/layouts/MarkdownLayout.astro'
title: Snapshot
network: Mainnet
icon: lumen
chain: lumen
version: v1.3.0
---
Update Everyday

```
sudo apt install lz4 -y
```
```
sudo systemctl stop lumen
```
```
cp $HOME/.lumen/data/priv_validator_state.json $HOME/.lumen/priv_validator_state.json.backup
```
```
lumend tendermint unsafe-reset-all --home $HOME/.lumen --keep-addr-book
```
```
curl -L https://snapshot-t.vinjan.xyz/lumen/latest.tar.lz4 | lz4 -dc - | tar -xf - -C $HOME/.lumen
```
```
mv $HOME/.lumen/priv_validator_state.json.backup $HOME/.lumen/data/priv_validator_state.json
```
```
sudo systemctl restart lumend
sudo journalctl -u lumend -f -o cat
```

---
layout: '~/layouts/MarkdownLayout.astro'
title: Live Peers
network: Testnet
icon: pchain
chain: push_42101-1
version: v0.0.12
---
Automatically adding Live peers to your config.toml file
```
peers="$(curl -sS https://rpc-test.pchain.vinjan-inc.com:443/net_info | jq -r '.result.peers[] | "\(.node_info.id)@\(.remote_ip):\(.node_info.listen_addr)"' | awk -F ':' '{print $1":"$(NF)}' | sed -z 's|\n|,|g;s|.$||')"
sed -i.bak -e "s/^persistent_peers *=.*/persistent_peers = \"$peers\"/" $HOME/.pchain/config/config.toml
```

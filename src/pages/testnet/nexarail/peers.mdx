---
layout: '~/layouts/MarkdownLayout.astro'
title: Live Peers
network: Testnet
icon: nxrl
chain: nexarail-testnet-1
version: v0.1.0-rc1-validator-recovery-hotfix
---
Automatically adding Live peers to your config.toml file

```
peers="$(curl -sS https://rpc-t.nexarail.vinjan-inc.com:443/net_info | jq -r '.result.peers[] | "\(.node_info.id)@\(.remote_ip):\(.node_info.listen_addr)"' | awk -F ':' '{print $1":"$(NF)}' | sed -z 's|\n|,|g;s|.$||')"
sed -i -e "s/^persistent_peers *=.*/persistent_peers = \"$peers\"/" $HOME/.nexarail/config/config.toml
```
